os: osx
osx_image: xcode11.4.1
language: swift
before_install:
  - yes | gem update --system --force
  - gem install bundler
install: 
  - bundle install
script:
  - bundle exec fastlane build_and_test
